# ----------------------------------------------------------------------
# Zig Build Artifacts (Recursive)
# ----------------------------------------------------------------------
# โฟลเดอร์ Cache ขนาดใหญ่ที่ Zig สร้างขึ้น (ต้อง Ignore เด็ดขาด)
.zig-cache/

# โฟลเดอร์ Output ที่ได้จากการ Build (exe, lib)
zig-out/

# ----------------------------------------------------------------------
# Windows & OS Files
# ----------------------------------------------------------------------
# ไฟล์ขยะของ Windows
Thumbs.db
ehthumbs.db
Desktop.ini
$RECYCLE.BIN/

# ไฟล์ขยะของ Mac (เผื่อเปิดในเครื่องอื่น)
.DS_Store

# ----------------------------------------------------------------------
# IDE & Editor
# ----------------------------------------------------------------------
# VS Code (ถ้าอยากเก็บ config ของโปรเจกต์ ให้ลบบรรทัดนี้ออก)
.vscode/

# IntelliJ / JetBrains
.idea/

# Vim / Editor temp files
*.swp
*~

# ----------------------------------------------------------------------
# Error Logs (Optional)
# ----------------------------------------------------------------------
*.log